git init
git status
git diff
git add [file or folder name]
git add . // adding all changes
git commit -m "comit message"
git log
git log -n // where n is the integer number
git show <commit_id>


git remote add <remote_name> <remote_url>
git clone git@github.com:ZviadiOlegovich/project_one.git

git push <romote_name> <branch_name>
git push origin main
git pull <romote_name> <branch_name>

git checkout -b <new_branch_name>
git checkout <commit hash>

git stash

git cat-file -t <узнать тип файла>
git cat-file -з <узнать содержимое файла>